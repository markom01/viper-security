---
import type { NavLink, PageContent } from "../../types";

interface Props {
  pageContent?: PageContent;
  footerItems?: NavLink[];
  brandName?: string;
  tagline?: string;
  phoneItaly?: string;
  phoneSpain?: string;
  /** Social profiles. Verified real handles per site; bare roots disabled. */
  social?: { icon: string; href: string; label: string }[];
}

const { pageContent, footerItems, brandName, tagline, phoneItaly, phoneSpain, social } = Astro.props as Props;
import STRINGS from "../../config/strings";
import { resolveLabels } from "../../config/labels";
const s = resolveLabels(pageContent, STRINGS);
const b = pageContent?.branding;
const thisYear = new Date().getFullYear();
// Footer links as data. Default = VIPER menu. A site passes its own via footerItems.
const links = footerItems ?? [
  { label: s.nav.chauffeur, href: "/" },
  { label: s.nav.luxuryCars, href: "/luxury-cars" },
  { label: s.nav.armouredVehicles, href: "/armoured-vehicles" },
  { label: s.nav.about, href: "/#about" },
  { label: s.nav.services, href: "/#services" },
  { label: s.nav.fleet, href: "/#fleet" },
  { label: s.nav.howItWorks, href: "/#how-it-works" },
  { label: s.nav.pricing, href: "/#pricing" },
  { label: s.nav.contact, href: "/#contact" },
  { label: "Privacy Policy", href: "/privacy-policy" },
];

const cleanPhone = (phone?: string) => (phone || "").replace(/\D/g, "");
const phoneSep = s.footer.phoneSeparator;
const mapEmbedUrl = pageContent?.map_embed_url;
const isAllowedMapUrl = (u?: string) => !!u && (u.startsWith("https://www.google.com/maps/embed/") || u.startsWith("https://maps.google.com/"));
---

<footer class="footer">
  <div class="content-wrapper">
    <div class="footer-content-wrapper">
      <div class="footer-middle">
        <div class="footer-text-wrapper">
          <a href="/" class="logo-link">
            <span class="brand-text">{brandName}</span>
          </a>
          <p class="pure-text">
            {
              phoneItaly && (
                <a
                  href={`https://wa.me/${cleanPhone(phoneItaly)}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  aria-label={`WhatsApp ${phoneItaly}`}
                  class="phone-wa-link">{phoneItaly}</a
                >
              )
            }
            {phoneItaly && phoneSpain ? phoneSep : null}
            {
              phoneSpain && (
                <a
                  href={`https://wa.me/${cleanPhone(phoneSpain)}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  aria-label={`WhatsApp ${phoneSpain}`}
                  class="phone-wa-link">{phoneSpain}</a
                >
              )
            }
          </p>
          {
            social?.length ? (
              <div class="social-links-wrapper">
                {social.map((link) => (
                  <a
                    href={link.href}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="social-link"
                    aria-label={link.label}
                  >
                    <img
                      src={`/images/${link.icon}`}
                      width="20"
                      height="20"
                      loading="lazy"
                      decoding="async"
                      alt=""
                      aria-hidden="true"
                    />
                  </a>
                ))}
              </div>
            ) : null
          }
        </div>
        <div class="footer-links-wrapper">
          <div class="footer-links-column">
            <h2 class="footer-links-heading">{s.footer.pagesHeading}</h2>
            <div class="footer-links">
              {
                links.map((l) => (
                  <a href={l.href} class="footer-link">
                    {l.label}
                  </a>
                ))
              }
            </div>
          </div>
        </div>
        {
          isAllowedMapUrl(mapEmbedUrl) && (
            <div class="footer-map-wrapper">
              <iframe
                src={mapEmbedUrl}
                width="100%"
                height="220"
                style="border:0"
                loading="lazy"
                referrerpolicy="no-referrer-when-downgrade"
                title="Location map"
                allowfullscreen
              />
            </div>
          )
        }
      </div>
    </div>
  </div>
</footer>
<style>
  .footer-luxury-tagline {
    font-family: var(--font-families--accent-font);
    font-size: 1rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--colors--gold-bright);
    margin-top: 6px;
  }
  .brand-text {
    font-family: var(--font-families--accent-font);
    font-size: 1.5rem;
    letter-spacing: 0.08em;
    color: var(--colors--gold-bright);
    white-space: nowrap;
  }
  .footer-motto {
    font-size: 1rem;
    letter-spacing: 0.08em;
    color: var(--colors--slate);
    margin-top: 4px;
  }
  .phone-wa-link {
    color: inherit;
    text-decoration: none;
    transition: color 0.2s ease;
  }
  .phone-wa-link:hover,
  .phone-wa-link:focus-visible {
    color: var(--colors--gold-bright);
    text-decoration: underline;
  }
  .footer-link:hover {
    color: var(--colors--gold-bright);
  }
</style>
